# ── Audio Source Separation ──
demucs>=4.0.0
torchcodec>=0.13.0

# ── Vocal Transcription ──
# basic-pitch is installed via --no-deps to bypass the TensorFlow dependency conflict
# on Python 3.12 (since it runs perfectly using ONNX on Windows).
# We explicitly list its non-TensorFlow dependencies here:
onnxruntime>=1.16.0
mir-eval>=0.8.2
resampy>=0.4.2

# ── Piano Transcription ──
piano-transcription-inference>=0.0.5

# ── Audio Analysis ──
librosa>=0.10.0
soundfile>=0.12.0

# ── MIDI Manipulation ──
pretty_midi>=0.2.10

# ── Chord Detection (Path B) ──
numpy>=1.24.0
scipy>=1.10.0

# ── Configuration & Validation ──
pydantic>=2.0
pyyaml>=6.0

# ── Logging ──
loguru>=0.7.0

# ── CLI ──
click>=8.1.0

# ── Testing ──
pytest>=7.4.0
pytest-cov>=4.1.0

# ── Video Rendering (Python fallback) ──
moviepy>=2.0
Pillow>=10.0

# ── Utilities ──
tqdm>=4.65.0
